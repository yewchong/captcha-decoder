===Environment Setup===

- Put ALL native library files in dll folder into the library path.

Windows
On Windows, the system path is part of the library path. So you can put the DLL into
> AspriseOCR.dll
> DevIL.dll
> ILU.dll
any of the folder in the system path, e.g., C:/Windows/System32.